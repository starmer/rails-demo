<% form_for item do |f| %>
  <%= f.text_field :title %> <%= f.submit "Save" %>
  <%= f.submit "Cancel", :id => item.id.to_s + "-cancel" %>
  <a href="<%= url_for :controller => 'items', :action => 'delete', :id => item.id.to_s   %>" id="<%=item.id.to_s + '-delete' %>">
    Delete
  </a>
<% end %>