<% form_for list, :html => {:id => 'list-form'} do |f| %>
  <%= f.text_field :title %> <%= f.submit "Save" %>
  <%= f.submit "Cancel", :id => "list-cancel" %>
  <a href="<%= url_for :controller => 'lists', :action => 'delete', :id => list.id.to_s   %>" id="list-delete">
    Delete
  </a>
<% end %>